import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    background: 'white',
    width: '15rem',
    margin: '5px',
    padding: '5px',
    borderRadius: '4px',
  },
  title: {
    fontSize: '0.8rem',
  },
});

export default useStyles;
