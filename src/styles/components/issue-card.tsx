import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  root: {
    background: "gray",
    margin: "3px",
    padding: "5px",
    borderRadius: 3,
  },
});

export default useStyles;
