import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  root: {
    background: "gray",
    width: "15rem",
    margin: "5px",
    padding: "5px",
    borderRadius: "4px",
  },
});

export default useStyles;
