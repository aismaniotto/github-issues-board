import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  root: {
    background: "red",
    height: "100vh",
    width: "20em",
    margin: "10px",
    borderRadius: 3,
  },
});

export default useStyles;
