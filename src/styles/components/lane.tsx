import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  root: {
    background: "red",
    padding: " 2px",
    margin: "3px",
    borderRadius: "4px",
  },
});

export default useStyles;
