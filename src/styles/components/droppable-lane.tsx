import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    background: '#e5e4e2',
    padding: '2px',
    margin: '3px',
    height: 'auto',
    borderRadius: '4px',
  },
  title: {
    margin: '5px',
    fontSize: '1rem',
  },
});

export default useStyles;
